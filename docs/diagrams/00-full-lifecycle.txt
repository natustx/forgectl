================================================================================
                     FORGECTL — FULL DEVELOPMENT LIFECYCLE
================================================================================

  A spec-driven development harness that compiles planning documents into
  production code through three sequential phases, each governed by a state
  machine with iterative AI-powered evaluation loops.

                         ┌──────────────────────┐
                         │    Planning Docs /    │
                         │    User Input         │
                         └──────────┬───────────┘
                                    │
                                    ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                                                                         │
  │   PHASE 1: SPECIFYING                                                   │
  │   Role: Systems Architect                                               │
  │                                                                         │
  │   Input:  spec-queue.json                                               │
  │   Output: specs/*.md (permanent contracts)                              │
  │                                                                         │
  │   ┌───────┐  ┌────────┐  ┌───────┐  ┌──────────┐  ┌────────┐          │
  │   │ORIENT ├─►│ SELECT ├─►│ DRAFT ├─►│ EVALUATE ├─►│ ACCEPT │          │
  │   └───────┘  └────────┘  └───────┘  └────┬─────┘  └───┬────┘          │
  │                                           │            │                │
  │                                      FAIL │            │ more specs     │
  │                                           ▼            │                │
  │                                      ┌────────┐       │                │
  │                                      │ REFINE ├───┘   │                │
  │                                      └────────┘       │                │
  │                                                        ▼                │
  │   ┌──────┐  ┌───────────┐  ┌──────────────┐  ┌─────────────┐          │
  │   │ DONE ├─►│ RECONCILE ├─►│ RECONCILE    ├─►│ RECONCILE   │          │
  │   └──────┘  └───────────┘  │ EVAL         │  │ REVIEW      │          │
  │                             └──────────────┘  └──────┬──────┘          │
  │                                                      │                  │
  │                                                      ▼                  │
  │                                                ┌──────────┐            │
  │                                                │ COMPLETE │            │
  │                                                └────┬─────┘            │
  └─────────────────────────────────────────────────────┼───────────────────┘
                                                        │
                     ┌──────────────────────────────────┘
                     │  PHASE_SHIFT  (context refresh)
                     │  forgectl advance --from plan-queue.json
                     ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                                                                         │
  │   PHASE 2: PLANNING                                                     │
  │   Role: Staff Engineer                                                  │
  │                                                                         │
  │   Input:  plan-queue.json + specs/*.md                                  │
  │   Output: plan.json + notes/*.md                                        │
  │                                                                         │
  │   ┌───────┐  ┌───────────┐  ┌───────────┐  ┌──────────────┐           │
  │   │ORIENT ├─►│STUDY_SPECS├─►│STUDY_CODE ├─►│STUDY_PACKAGES│           │
  │   └───────┘  └───────────┘  └───────────┘  └──────┬───────┘           │
  │                                                     │                   │
  │                   ┌────────┐  ┌───────┐             │                   │
  │                   │ REVIEW │◄─┘       │◄────────────┘                   │
  │                   └───┬────┘          │                                 │
  │                       ▼               │                                 │
  │                   ┌───────┐    ┌──────────┐                             │
  │                   │ DRAFT ├───►│ VALIDATE │ (if plan invalid)           │
  │                   └───┬───┘    └────┬─────┘                             │
  │                       │             │                                   │
  │                       ▼             │                                   │
  │                  ┌──────────┐◄──────┘                                   │
  │                  │ EVALUATE │                                           │
  │                  └────┬─────┘                                           │
  │                       │                                                 │
  │              FAIL     │     PASS                                        │
  │            ┌──────────┼──────────┐                                      │
  │            ▼                     ▼                                       │
  │       ┌────────┐           ┌────────┐                                   │
  │       │ REFINE ├──┘        │ ACCEPT │                                   │
  │       └────────┘           └───┬────┘                                   │
  │                                │                                        │
  └────────────────────────────────┼────────────────────────────────────────┘
                                   │
                     ┌─────────────┘
                     │  PHASE_SHIFT  (context refresh)
                     │  Mutates plan.json: adds passes + rounds fields
                     ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                                                                         │
  │   PHASE 3: IMPLEMENTING                                                 │
  │   Role: Senior Software Engineer                                        │
  │                                                                         │
  │   Input:  plan.json (with passes/rounds)                                │
  │   Output: Production code + eval reports                                │
  │                                                                         │
  │   ┌───────┐  ┌───────────┐  ┌───────────┐  ┌──────────┐  ┌────────┐  │
  │   │ORIENT ├─►│IMPLEMENT  ├─►│IMPLEMENT  ├─►│ EVALUATE ├─►│ COMMIT │  │
  │   └───────┘  │ (item 1)  │  │ (item N)  │  └────┬─────┘  └───┬────┘  │
  │              └───────────┘  └───────────┘       │             │        │
  │                                            FAIL │             │        │
  │                                                 ▼             │        │
  │                                           ┌───────────┐      │        │
  │                                           │IMPLEMENT  │      │        │
  │                                           │(round 2+) ├──┘   │        │
  │                                           └───────────┘      │        │
  │                                                               │        │
  │                                    more batches ◄─────────────┘        │
  │                                         │                              │
  │                                         ▼                              │
  │                                    ┌────────┐                          │
  │                                    │  DONE  │                          │
  │                                    └────────┘                          │
  │                                                                         │
  └─────────────────────────────────────────────────────────────────────────┘


  ═══════════════════════════════════════════════════════════════════════════
  KEY
  ═══════════════════════════════════════════════════════════════════════════

  ──►  State transition (forgectl advance)
  │    Sequential flow
  ◄──  Loop back / retry

  PHASE_SHIFT    Hard stop between phases; engineer refreshes context
  EVALUATE       AI sub-agent reviews work against 11 evaluation dimensions
  REFINE         Fix deficiencies found in evaluation
  COMMIT         Hard stop; engineer must git commit before continuing
  GUIDED         Optional pause for human discussion (--guided flag)

  Round Enforcement (from .forgectl/config [phase].eval):
    eval.min_rounds   PASS below minimum forces another REFINE cycle
    eval.max_rounds   FAIL at maximum forces acceptance (prevents infinite loops)

  Config & State:
    .forgectl/config                  TOML config file (phase-scoped)
    .forgectl/state/forgectl-state.json State file (JSON)
