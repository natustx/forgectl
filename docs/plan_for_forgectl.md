### Planner
- the planner creates the SPEC_MANIFEST.md this should not be allowed.
- planner does not know what the SPEC_MANIFEST should look like.
- can get rid of the implemenation-phases.md
- might want to make a research skill for implementation-stack and implemenation-phases...  or something like that
    - implemenatation phases sounds like it would go with the implementation planning.
- shared/schemas.md should be used in the spec alignment step....
- get the SPEC_MANIFEST issue completed....


### Implement from specs
- needs to state that the diff is in the commit hash of specs
- should rename to quick implementation
- should reinforce that we have already specs generated and needs this to begin work.  if not provided do not work.
- need to ensure that we align this with the application and forgectl, since it kinda works outside forgectl.


### Specs
- need a SKILL.md file



How do multiple skills work with each other?
   — probably set it up to select what we do, and there is a workflow that one must follow.



- We generate them from plans
- We update them from talking to the agent
- We can reverse engineer the code base to get these.
- Once we have them we have a plan, or an idea we have search for them to identify which ones we work on. 
- We have to decide when to create new spec files.
- Merge them, refactor them, and update them
- Before we commit we can refactor the specs. Merge or whatever.


- Decide — update and create


- the forgectl-state-schema file is really heavy,  we need to lighten the load of the file, so that it shorter.
- spec format needs to be consolidated as well, and lightened as well too.

- integrate the `search-specs-for-specifying.md` into the forgectl so that it is a prereq for specifing step...

- no eval sub-agent prompt template  or at least it is not in the skill....   but i think this should be an agent.
- no The reconciliation eval prompt template





